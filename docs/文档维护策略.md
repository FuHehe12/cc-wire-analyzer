# CC Wire Analyzer 文档维护策略

> 读者：项目维护者（单人 + AI 辅助）和想理解文档组织的贡献者。
> 这是一份**元文档**——讲文档本身怎么维护，与 [架构总览.md](架构总览.md)（讲软件怎么搭起来）
> 是不同维度的话题。
> 配套：[界面导览.md](界面导览.md) / [API契约.md](API契约.md) / [AI_USAGE.md](AI_USAGE.md) /
> [架构总览.md](架构总览.md) / README.md（三语）。

## 引子：文档腐化是指数级的

> **功能增长是线性以上的，文档腐化是指数级的**——每加一个功能，旧文档都要被重读、判断是否
> 需要改、改对地方。任何一个环节漏掉，文档就和代码分叉，下次读者（人或 AI）就被误导。

这不是假设——cc-wire-analyzer 当前已经有具体证据。下面三处腐化都是 260725 多视角扒查时**实际
发现**的，不是文档自检发现的：

| 文档 | 腐化表现 | 怎么发现的 |
|------|---------|----------|
| `docs/API契约.md` | 缺 `/api/health/config` 和 `/api/diagnose/errors` 两个新端点；`usage` 字段名双轨（契约写短名，实际录的是 Anthropic 全名） | 后端 API 子代理扒查 |
| `CLAUDE.md` 的「死配置」段 | 描述滞后——`redact_headers` UI 已删、`auto_start_proxy` 前端已接线，不再是死配置 | 前端子代理扒查 |
| `CLAUDE.md` 的验证清单 | 写了 `uv run python src/settings_guard_selftest.py`，但文件**不存在**——自测嵌在 `settings_guard.py --self-test` 里 | 外壳子代理扒查 |

注意一个规律：**腐化都是子代理扒查实际代码时发现的，项目目前没有"文档对账"机制**。等读者
撞上时已经晚了一步——这就是为什么需要这套策略。

---

## 当前文档矩阵（盘点现状）

cc-wire-analyzer 当前的文档分成两类（开源仓库 vs 本地维护），各覆盖不同受众：

### 开源仓库（进 GitHub）

| 文档 | 定位 | 读者 | 当前规模 |
|------|------|------|----------|
| `README.md` / `.zh.md` / `.ja.md` | 项目门面，三语 | 第一次接触的人 | ~200 行 |
| `docs/API契约.md` | 接口规格 | 前端开发者 / agent | 21KB |
| `docs/AI_USAGE.md` | AI agent 入门 | AI agent（中文，agent 也能读）| 13KB |
| `docs/界面导览.md` | 人看到什么、数据从哪来、哪些能力没有 UI 入口 | 人类用户（含非程序员） | ~390 行 |
| `docs/架构总览.md` | 软件怎么搭起来 | 想贡献代码 / 复盘架构的人 | ~700 行 |
| `docs/报文解读.md` | CC 的 7 种请求逐一解读 + 识别方法论 | 看不懂请求 chip 的使用者 | ~270 行 |
| `docs/开发指南.md` | **改代码时不能破什么**（约定单一真源） | 改这个项目代码的人和 AI | 19KB |
| `docs/问题域手册.md` | 换个 harness 做同类工具要解决哪些问题 | 要做 codex/opencode 版的人和 AI | 31KB |
| `CONTRIBUTING.md` | 环境搭建 / 构建 / 提 PR（约定指向开发指南） | 开源贡献者 | 薄壳 |
| `CHANGELOG.md` / `.zh.md` | 版本变更日志 | 升级用户 / 维护者 | 滚动增长 |
| `LICENSE` | 协议（MIT） | 任何人 | 标准 |
| `docs/文档维护策略.md`（本篇） | 元方法论 | 维护者 / 贡献者 | — |

### 本地维护（`.gitignore` 排除）

| 文档 | 定位 | 读者 |
|------|------|------|
| `CLAUDE.md` | **本机特有**：真流量采集姿势、内部引用、本地/开源边界（**开发约定已迁 `docs/开发指南.md`**）| AI（看项目的人） |
| `issues/open/` + `issues/closed/` | 改动的前因/方案/决策记录 | 维护者（含 AI） |

**两套文档的边界是分受众**：开源仓库面向公众（去内部痕迹、不含本机路径）；本地维护面向项目
内部（含 trace-analyzer / 智谱 / 本机路径等内部引用）。这条治理线在 `issues/closed/260712_去AI痕迹与本地维护规范.md`
确立。

---

## 适合本项目的策略

cc-wire-analyzer 是**单人维护 + AI 辅助 + 开源 + 多语言读者**的项目。最致命的失败模式是
**单人维护断裂**——维护者一旦忘了一个细节，没人补位，文档就分叉。所以策略应该往**让 AI 能
自检**那端倾斜，而不是模仿企业级 docs-as-code 方案。

### 策略一：SSOT 单一真源（最重要）

> **每个事实只在一处定义，其他地方引用**。文档之间互相矛盾就是文档无 SSOT 的后果——
> cc-wire-analyzer 已经有现成证据：契约说 `usage` 字段短名，AI_USAGE 说全名，代码里归一后
> 才短名。三处定义，三处真值。

代码里项目已经在做 SSOT（`usage_norm` 单一真源、`IDX_SCHEMA` 版本号），但**文档没有 SSOT
机制**。要做的是：

- **每个枚举/字段名/规则 code 只在一处权威定义**。候选位置：模块开头 docstring 或专门的
  `docs/字段字典.md`。其他文档提到这个字段时**链接过去**，不重复写定义。
- **每次字段集变化，SSOT 处定义、其他文档引用**。这跟代码里 `IDX_SCHEMA` bump 是同一种
  哲学——一处变，处处对得上。

具体落地：

| 真源类型 | 当前散落处 | 应集中的位置 |
|---------|-----------|-------------|
| API 端点列表 | API契约 + AI_USAGE + 界面导览 | `API契约.md` 唯一定义，其他文档引用 |
| kind 枚举 | classifier.py docstring + AI_USAGE + 界面导览 | classifier.py docstring 是 SSOT，文档引用 |
| err_kind 枚举 | proxy.py + diagnose.py + API契约 | proxy.py docstring 是 SSOT |
| doctor 8 条规则 code | doctor.py + 界面导览 + AI_USAGE | doctor.py 顶部的规则表是 SSOT |
| 字段名（usage 短名 vs 全名） | 契约说短名、AI_USAGE 说全名、代码归一后才短名 | 统一为"录制格式=全名、API 出参=归一后短名"，在 SSOT 处显式说清 |
| **开发约定**（不变量 / 惯犯 bug / 自测 / 前端规则） | ~~CLAUDE.md + CONTRIBUTING + 架构总览**三处**~~ | `docs/开发指南.md` 唯一定义，另两处改引用（260729 落地，见腐化 #13/#14）|

### 策略二：三层结构（导航 / 参考面 / 价值层）

避免单一大 README（越长越没人读）和功能文档矩阵（碎片化、引用腐烂）两个极端，按层级组织：

```
导航层（README）：项目门面 + Quick start + 链接
   │
   ▼
参考面（API契约 / 字段字典）：规格，机器可校验
   │
   ▼
价值层（界面导览 / 架构总览 / AI_USAGE）：叙事，按受众分
```

| 层 | 文档 | 写什么 | 何时更新 |
|----|------|--------|----------|
| 导航 | README.md（三语） | 一句话定位 + Quick start + 链接到价值层 | 加新功能时改 Quick start |
| 参考 | API契约.md / 字段字典 | 接口规格 / 枚举值 / 字段定义 | API/字段变化时必改（CI 校验） |
| 价值 | 界面导览 / 架构总览 / AI_USAGE | 按**受众**讲为什么和怎么用 | 受众视角有大变化时改 |

**价值层按受众分**，不按功能分。当前已有：
- 给人：`界面导览.md`（界面看什么）/ `报文解读.md`（CC 的 7 种请求报文是什么）
- 给开发者/复盘者：`架构总览.md`（怎么搭的）/ `开发指南.md`（改的时候不能破什么）
- 给 AI agent：`AI_USAGE.md`
- 给未来的同类项目：`问题域手册.md`（换 harness 时哪些结论还成立）

**260729 补一条判据：受众相同也要按「触发时机」分开。** 架构总览和开发指南读者都是开发者，
但一个是"我要读懂这个项目"、一个是"我正要动手改"——合在一份里，动手的人得翻过几百行架构叙事
才找到不变量清单。四份开发向文档的触发时机是：读懂→架构总览 / 动手→开发指南 / 改文档→本篇 /
做同类项目→问题域手册。

**新增功能时不要急着开新文档**——先想这个功能的读者是谁，归到现有受众文档里。比如失败聚合
加 UI 入口后，应该更新到 `界面导览.md` 的"第二层"段，而不是单开"失败聚合功能文档"。

### 策略三：每份文档自带"自检句"

在每个模块/功能文档的对应章节末尾写一句**给未来 AI/人的提示**：

> 如果你改了 X，必须同步改这里。

这是给未来维护者的提示，比 CI 校验更柔但覆盖更广（CI 校验得了链接和端点，校验不了语义）。

例：

- `docs/API契约.md` 的 `/api/proxy/status` 段末尾：「**自检**：加新字段时必须同步更新
  `_proxy_state()` 函数 + 此契约 + AI_USAGE 的字段表。」
- `docs/界面导览.md` 的视图章节末尾：「**自检**：加新视图/新横幅时必须更新此章节的视图清单
  + [架构总览.md] 的'第三层：交互'段。」
- `docs/开发指南.md` 的惯犯 bug ① 末尾：「**自检**：加新配置项时必须确认它在前端、后端
  `config.py`、实际消费点三处都接通——任何一处断了就是新的死配置。」

### 策略四：issue 先行扩展到文档同步

cc-wire-analyzer 已经有"改代码先开 issue"的纪律（**定义在 [开发指南.md](开发指南.md) 第八节**，
本篇不重复；本地的目录结构与路径细节在 `CLAUDE.md`，不上传开源仓库）。本篇只加文档相关的这一条：

> **任何带新功能/新字段的 issue，必须开"文档同步" sub-task**。issue closed 时核对该功能
> 涉及的所有文档提及处。

具体执行：

1. 开 issue 时在「产出」段列出"涉及的文档"清单（README / API契约 / 界面导览 / 架构总览 /
   AI_USAGE / 开发指南 / 问题域手册 / CLAUDE.md，按需勾选）
2. 改代码同时改文档（同一个 PR / 同一次本地改动）
3. close issue 前回读"自检句"，确认每处都同步

这与"issue 是迭代经历"的现有定位同源——文档同步也是迭代经历的一部分。

### 策略五：AI 可自检（项目特有的杠杆）

cc-wire-analyzer 的定位是"不只给人看，AI 也能自己开、自己查"。文档也应该让 AI 能自检。具体
做法：

- **结构化字段优先**：API/字段/规则用 markdown 表格（机器可解析），叙事用散文（人可读）。
- **写一个 `tools/doc_audit.py` 脚本**（轻量，作为新 issue 候选）：扫"代码定义的端点 vs 文档
  列出的端点"对账、扫"代码里的枚举 vs 文档里的枚举"。这跟 `tools/lane_probe.py` 是同一种
  "摊开证据核对"的范式。
- **AI 跑文档对账**：AI agent 调 `cli.py doctor` 拿规则 code 列表，与 `docs/界面导览.md` 的
  体检章节对账——能发现"代码加了规则但文档没列"的滞后。

---

## 执行清单：每加一个功能时跑一遍

新增功能 / 新增字段 / 新增规则时，按顺序过：

```
□ 1. 这个功能的读者是谁？归到现有受众文档（界面导览/架构总览/AI_USAGE），不轻易开新文档
□ 2. 涉及的 SSOT 在哪？字段/枚举/code 加到 SSOT 处（通常是模块 docstring）
□ 3. 文档同步：其他文档怎么引用这个 SSOT？写引用，不重复定义
□ 4. 自检句：在该章节末尾加"如果你改了 X，必须同步改这里"
□ 5. issue 同步：开"文档同步" sub-task，列涉及的文档清单
□ 6. CI 校验（如果加了端点/字段）：tools/doc_audit.py 跑一遍（待实现）
□ 7. 三语更新（如果用户可见字符串）：README 三语 + 界面文案三语 + i18n key
```

---

## 腐化清单（#1-12 为 260726 发现，#13-14 为 260729 发现）

260726 多视角扒查发现的 12 处文档腐化。v0.4.0 文档 catch-up 修了大部分，v0.4.1 补完 #5/#8。
下表标状态供回溯（#6/#7 在 CLAUDE.md 域，本地维护用、不上开源仓库）：

| # | 文档 | 腐化 | 修复 | 状态 |
|---|------|------|------|------|
| 1 | `docs/API契约.md` | 缺 `/api/health/config` 端点 | 补端点 + 字段表 | ✅ v0.4.0 |
| 2 | `docs/API契约.md` | 缺 `/api/diagnose/errors` 端点 | 补端点 + 字段表 | ✅ v0.4.0 |
| 3 | `docs/API契约.md` | `usage` 字段名双轨 | 显式说明录制全名 / API 短名 | ✅ v0.4.0 |
| 4 | `docs/API契约.md` | translate/explain 写非流式 | 改 SSE 协议描述 | ✅ v0.4.0 |
| 5 | `src/app.py` | start 响应死字段 `orphan_recovered` 恒 null | 删代码死字段（前端只读 `_at_startup`）| ✅ v0.4.1 |
| 6 | `CLAUDE.md` 死配置段 | 描述滞后 | 更新为历史参考 | ✅ v0.4.0（CLAUDE.md 域）|
| 7 | `CLAUDE.md` 验证清单 | `settings_guard_selftest.py` 不存在 | 改 `--self-test` | ✅ v0.4.0（CLAUDE.md 域）|
| 8 | `docs/AI_USAGE.md` | 缺多个端点 | 补 stream + human-facing 注 | ✅ v0.4.1 |
| 9 | `docs/AI_USAGE.md` | usage 与契约不一致 | SSOT 化 | ✅ v0.4.0 |
| 10 | README.md | release 页面看不到 tag | ~~顶部加 Current version 行~~ → v0.4.1 改法反转：只留 Releases 链接（GitHub 自动指向最新） | ✅ v0.4.1（见下方注） |
| 11 | `docs/API契约.md` | 缺枚举完整列表 | SSOT 处定义 + 契约引用 | ✅ v0.4.0 |
| 12 | i18n 表 | doctor 只 3 条翻译 | 补齐 8 条三语 | ✅ v0.4.0 |
| 13 | `CONTRIBUTING.md` | 开发约定副本三处失真：自测停在 2 条（实际 6）、不变量停在 3 条（实际 8）、**两处写着「dev server 实时读模板」（错的）** | 停止复述，改指 `docs/开发指南.md` | ✅ v0.4.1（见下方注）|
| 14 | `docs/架构总览.md` | 第二份开发约定副本（8 不变量 / 防御性设计表 / 教训表 / 依赖图）；附录 C 写「issues/closed 27 条」（实际 45）、「界面导览 13 条优化机会」（该节已摘除）| 四段迁入开发指南，原处留引用；附录 C 改写 | ✅ v0.4.1 |

批量修复落地于 `issues/closed/260726_文档腐化批量修复.md`（v0.4.0）+ `issues/closed/260728_v0.4.1_文档优化.md`（v0.4.1 补 #5/#8）。
**这份清单本身也曾腐化**——v0.4.1 标完成状态后才重新可信，正好印证下面"策略一 SSOT"：对账清单自己也是文档，不更新就会失效。

**#13 是最贵的一条，因为它真的骗到了人**：CONTRIBUTING 写着「the dev server reads templates
live, no rebuild needed」，260729 有人（AI）照做，对着 Jinja 缓存的旧页面查了半天改动为何不
生效。副本不是"迟早会过时"的隐患，它是**已经在误导读者**的现行事实。

#13 和 #14 合起来暴露一个当初没看见的结构问题：**开发约定同时有三份**（CLAUDE.md 本地 +
CONTRIBUTING 公开摘要 + 架构总览四个章节），而策略一的 SSOT 表当初只盘点了 API 端点、枚举、
字段名这些"数据型真源"，**漏了"约定型真源"**。260729 把它补进了表里。

**#10 的修法在 v0.4.1 被推翻，值得单独记一笔**：为让 README 页面显示版本而手写的 `Current version: vX.Y.Z` 行，上线后**第一次发版就腐化了**（v0.4.0 已发布，三份 README 仍写 v0.3.2，靠 260728 的三份独立审计交叉确认才发现）。原因正是策略一要消灭的东西——版本号的唯一真源是 git tag / GitHub Releases，README 抄一份就必然分叉，而"每次发版改三个文件里的一行字"这种义务没人守得住。现在只留 `[Releases](../../releases)` 链接，由 GitHub 自动指向最新，代价是读者多点一次。

**这条教训在 260729 又被验证了两次**（#13 CONTRIBUTING 的约定摘要、#14 架构总览的不变量章节），
三个实例形态各异——一行版本号、一段英文摘要、四个完整章节——但病因完全相同：

> **给腐化开的药方如果需要人工定期同步，它自己就是下一处腐化。**
> 优先选零维护的指针（链接、自动生成、真源引用），而不是可维护的副本。
> 判据很简单：**这份内容有没有第二个地方也写了？** 有就是副本，无论它当初抄得多准。

---

## 防腐化的边界

最后说一个反模式。**不是所有文档滞后都该修**——有些滞后是合理的代价：

- **commit history 的 `Co-Authored-By: Claude` trailer**：开源时讨论过，改写要 rebase 已 push
  的 tag，成本高收益低，**暂不动**（`issues/closed/260712_去AI痕迹与本地维护规范.md` 的决策）
- **macOS 真机测试覆盖度**：维护者在 Windows，macOS 靠 CI + 贡献者，不可能持续保持最新真实
  测试——这是已知欠债，CLAUDE.md 显式标注
- **CLAUDE.md 的"教训"段**：列的是项目结构性会再产的 bug 类型，不是当前 bug 清单——故意
  保留旧的（如 260713 的死配置描述）作为历史对照，但要明确标"已修复于 vX.X.X"避免误导

判断标准：**滞后是否会误导下一个读者？**——是则必修；只是过时但读者不会据此做错判断的，
可以延后。

---

## 一句话总结

> 单人维护的项目里，文档不是写给读者看的，是**写给三周后的自己看**——那时候你已经忘了这次
> 改动的所有上下文，只能靠文档对账代码。SSOT + 自检句 + issue 同步 + AI 自检，这四件套让
> "对账"这件事不必每次重新发明。
