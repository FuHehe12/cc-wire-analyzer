# 变更记录

开头是项目状态卡（只写现在），下面是未发布改动与最新一个版本，仅此一版；更早的全在[变更历史](CHANGELOG-history.md)。两份都只用中文，发版给 GitHub 用的三语正文在 [RELEASE_NOTES.md](RELEASE_NOTES.md)。大体经历查 `git tag -n`，每步改了什么查 `git log`，为什么这么定查本地 `issues/closed/`。怎么写这三份见项目根 `CLAUDE.md` 的「CHANGELOG 纪律」一节（本地开发文件，不随仓库发布）。

## 定位

本地 HTTP 代理桌面应用，透明记录 Claude Code 与上游的请求和响应，通过捕获列表、请求详情、时序图、快照对比与分析接口，帮助你理解模型收到的内容、子代理调用和异常原因。人用桌面界面看，分析 Agent 走本地 HTTP 接口看同一批记录。

个人自用的分析工具，需要什么就试什么，测试性功能经常大量增删。

## 原则与未决

不随版本变的两条口径：

- 产出就三样：桌面应用、供 Agent 使用的 HTTP/CLI 分析面、由本地源生成的产品说明书。
- 外部观察 Agent 负责解释，程序不自动判断语义正确或目标达成。

一直挂着、还没解的三个产品问题：

- **反复出现的故障没转成检查规则**。`/api/diagnose/trends` 只能辅助判断是否复发，是不是同一个病还得人看。
- **要不要用 Claude Code 本地日志修正轮次来源，没拍板**。开发期已经在用（对账探针靠 requestId 逐请求对齐），运行时读不读始终没定。（issue 260801_wire与jsonl的三条join链路）
- **存储是个大问题**：要在不影响使用的前提下尽可能压体积。（issue 260808_存储与聚合分层）

## 进行中与下一步

1. **文档梳理完就升 0.5.x**。大版本不按功能划——现实需要什么这个工具就开发什么，功能增删不构成版本节点；这次的节点是渐进式披露体系重建完、文档架构收口。
2. 手头没做完的：
   - `public/` 暂存的旧披露文件分批下线，剩 28 份待内容复核——冻结本就是重构时来不及处理合并的过渡措施，不是长期机制。（issue 260913_披露断面分批下线）
   - 文档瘦身还剩 `local/` 里的 markdown 没过，还没提 issue。
   - 观察者的可研只落地了一部分，其中「底图是时间顺序的一条轨迹」这个假设是被绕过去的，不是被验证的。（issue 260904_Agent观测者_需求与可研）
3. 收集跨机使用反馈，继续改善理解偏差的呈现与任务边界识别。首轮（macOS 全量验证）已完成，带回的修复项已落地，余下的设计取舍待定案。（issue 260915_mac验证带回的设计取舍）

## 未发布

### 变更

- 捕获详情页里加密思考那段说明收成一句话，不再占两行。（issue 260913_加密思考块与没有思考无法区分）

### 修复

- 极快连续创建快照时列表排序在两条读取路径上不一致，发版自检因此偶发失败，现在排序确定一致、不再偶发。（issue 260915_mac全量验证与索引重建断言偶发红的根因）
- 部分上游会显式声明内容未压缩，此前被误当成解码失败计进错误统计，现在按正常响应对待。（issue 260915_mac全量验证与索引重建断言偶发红的根因）
- 命令行敲了不存在的子命令时，此前一声不吭弹出图形界面把命令挂住，现在报错并退出。（issue 260915_mac全量验证与索引重建断言偶发红的根因）
- 快照对比两段完全相同时不再显示「正文差异」区块，不会再把相同行误读成一处差异。（issue 260915_mac全量验证与索引重建断言偶发红的根因）
- 程序抛出没接住的错误时，终端里不再只剩一个退出码，报错内容会照常打出来。（issue 260913_发版闸门偶发红且不给报错）
- 白板上会凭空多出一模一样的贴纸，批量清理碰到它们还会报删除失败，现在两件事都不再发生。（issue 260915_贴纸重复与批量删除失败与无忙碌反馈）
- 拖动一张贴纸和整理整块白板越来越慢，慢的程度跟着快照总量涨，现在与快照堆了多少无关。（issue 260915_贴纸重复与批量删除失败与无忙碌反馈）
- 备份、拖动、删除、整理、批量清理期间界面毫无动静，现在右下角会转圈说明在做什么、整理到第几张。（issue 260915_贴纸重复与批量删除失败与无忙碌反馈）
- 连点「备份当前录制」会真存下好几份一样的快照，现在一次备份没结束前的重复点击不再重复保存。（issue 260915_贴纸重复与批量删除失败与无忙碌反馈）
- 贴纸位置没存上时界面一声不吭、下次打开又回原处，现在会说明失败并把贴纸放回原位。（issue 260915_贴纸重复与批量删除失败与无忙碌反馈）
- 快照占用把分析结果也数成了快照，张数比白板上多；删快照时留下的旧语义分析文件现在也一并清掉。（issue 260915_贴纸重复与批量删除失败与无忙碌反馈）

## v0.4.38 - 2026-09-13

### 新增

- 捕获页多了「已冷藏」折叠区：很久没点开的日期自动收起来并再压小一截，点一下就解冻回日期栏。（issue 260913_实时分析排版收口与冷藏机制）
- 设置里能关掉自动冷藏或改天数（默认 7 天），也能在捕获页手动冷藏某一天。（issue 260913_实时分析排版收口与冷藏机制）

### 变更

- 实时分析页的字号收成六档，同一层级的内容不再各是各的大小。（issue 260913_实时分析排版收口与冷藏机制）

### 修复

- 实时分析页点开一个目标时，显示的编号与图上那张卡对不上，会把一件任务读成整体目标。（issue 260913_实时分析排版收口与冷藏机制）
- 捕获详情页里被上游加密的思考显示成空白，与模型压根没思考分不开。现在会标明已加密、读不到内容。（issue 260913_加密思考块与没有思考无法区分）

### 文档

- 官网补上实时分析的介绍（新章节、首屏文案与常见问题同步），请求详情一节加实际转发说明。（issue 260913_官网补实时分析与public建站部分下线）
- 建站源码与工具回归主线目录，public/ 冻结快照里对应的副本下线，原始摘要保留可核验恢复。（issue 260913_官网补实时分析与public建站部分下线）
- 披露断面开始分批下线：先删与主线逐字节一致的许可证副本和只差两处链接的构建手册，其余待内容复核。（issue 260913_披露断面分批下线）
- 变更记录改成三份：日常看的两份只用中文，发版给 GitHub 用的三语正文单独放一份，发版前对不上会直接挡住。（issue 260913_CHANGELOG分层收口与三语只走发版）
- 变更记录的写法要求收归开发入口文件一处，开发约定里那份互相打架的旧口径删掉。（issue 260913_CHANGELOG分层收口与三语只走发版）
- 变更记录顶部的状态卡从五节收成三节，不再抄当前版本的功能描述。（issue 260913_CHANGELOG分层收口与三语只走发版）

