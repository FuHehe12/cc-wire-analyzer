# 更新日志

> 本文件是 [`CHANGELOG.md`](CHANGELOG.md) 的中文翻译镜像，与英文版保持同步，英文版为源。
> 已发布版本的完整说明在 [`CHANGELOG-history.zh.md`](CHANGELOG-history.zh.md)。

## 项目速览

> 定位 / 当前状态 / 下一步——AI 接手快照。仅作导航；属规则/不变量的关键判断见本地 CLAUDE.md（开发约定）。条目里的 issue 路径是本地维护记录（gitignored，不在本仓库）。

- **定位**：本地 MITM 代理桌面应用，透明录制 Claude Code ↔ 上游端点的全部 HTTP 流量，填补 jsonl 日志与 OTLP 遥测看不到的链路级维度。双模式：GUI 给人看，`serve` 子命令暴露 headless HTTP API，让 AI agent 自己驱动排查——面向 agent 的说明书打包在产物里（`--help`，以及运行后的 `GET /api/ai-guide`），换一台机器用它不需要仓库。
- **当前状态**：**v0.4.21（2026-08-31）** 让今天的录制边录边压，不必等到第二天——录到阈值就把已写完的前缀封存成分片，跨天再合并回单个 pack，于是 28 倍的压缩比终于用在了「真正把磁盘填满的那一天」（实测单日 500~860MB）。切段守住了要害那条线：锁内改名冻住尾巴、锁外逐字节校验、校验通过才删源文件；失败则把这一天还原成切之前的样子。同一版把软件已经算好的分析开放给了外部 agent：那份指令的端点清单从 3 条扩到 8 条，并逐个点名 `trajectory` 里有什么——不点名等于没开放。还修掉了一个「归档被切过段的日子只会留下最后一截」的问题，它是在核对一句写反了的「顺带确认」时发现的。此前，**v0.4.20（2026-08-29）** 接着融合那一版，收掉真机试用一小时内冒出来的三件事：点八视图那一下屏幕在**同一帧**里就有变化（此前 138 节点那条要空白 5.3 秒，用户以为功能没了）、同一条录制不再每次打开都重算（**5.18 秒 → 0.03 秒**）、以及一个「AI 归纳」按钮**拆成两个**——一档一个、各自显示状态、各算各的钱，因为一个按钮按档分流等于把两件不同的事、两份不同的钱藏在同一个词后面。在它之前的 **v0.4.19（2026-08-29）** 是轨迹八视图不再像「另一个软件」的那一版：它现在与主界面共用三套外观、同一份打包字体、同一套字阶，嵌进分析页时没有第二个顶栏、也没有第二条滚动条（子页把高度报给父页，父页把可视区喂回来；换外观是原地重渲，而不是 reload 一张 payload 要算 ~20 秒的页）。这一页有史以来第一次对比度走查抓出 14 处不达标——全在两套浅色外观里、且全是同一个形状（`--agent` 被当文字色用了），按这个形状修完是**八视图 × 三套外观 = 0**。同一版还改掉了首版在事实上错的九处，包括 44 分钟的会话报出 129.9 小时辅助在途、页脚的「undefined 秒」、写死的原型录制工具数、以及「其后 -1 次是同类重试」。八视图本身也在这一版落地。上一版 **v0.4.18（2026-08-28）** 补上了让步级简报读不懂的那个缺口——归纳终于看得见你要什么、每次动作作用在什么上、以及返回了什么（此前 6.8 MB 工具输出进模型 0 字节）。简报改三段式、`got` 可字面校验；轮头把总目标与本轮在解决什么分开，「写了没验」由程序算而不是问模型。真数据 199 步端到端验过。上一版 **v0.4.17 已发（2026-08-27）**——这一版全部来自一次真机体验反馈，一次走完，主线是「AI 归纳从一次赌博变成一条可续跑的流水线」：管线倒置（步级先出、轮级与子代理结论都从步级简报卷起）、批次并发（同一条 126 步录制 26 分钟量级 → 196 秒）、逐层落盘与只补缺口的续跑（挖掉 20 步只跑 4 批 64 秒）、子代理每条线一句「任务/问题/解法/结果」。同轮修掉三个真 bug：自动更新重启后新进程**复用旧解压目录**（版本号是新的、界面是旧的，run.log 有完整现场）、思考原文抽屉按"贴住之后"算高度导致长译文整块落在视口外、重建索引把归纳文件当成幽灵贴纸。另加两条能力：抽屉宽度自适应+可拖拽（4K/笔记本/带鱼屏各自合适），以及**快照便携包**——快照连同 AI 归纳打成一个文件搬去另一台机器，不用重跑一遍。同一版还落了两件：API 浏览面上**需要参数的端点终于能点开了**——十条只写着"需路径参数"的死行（外加一个点下去必然报错的直链）现在各给一行可编辑 URL，预填的是本机自己的 rid 与 sid；这一页的外观也从两套补到三套，此前那次折叠一直在把「实验室日光」渲染成「经典暖灰」的色板，补三套时顺带做了它有史以来第一次配色走查，又量出三处不达 AA。上一版 **v0.4.16**（2026-08-27）讲两件事：分析视图从一副骨架长成了能读的叙事，以及那些让同一形状的 bug 连过三次的闸门。一条录制的**子代理终于成了故事的一部分**：`Task` 把活派进了别的请求，主线只留下一次工具调用和最后那份报告；现在视图回到录制里把它们捞回来，按派生它的那一步嵌套，并能钻进子代理任意一步的思考原文（实测：6 条线、6/6 都挂上了具体步骤、捞回 158 个子代理步骤）。步级简报改成**两段式**——一句话标题压着承载「为什么/发现了什么/放弃了什么」的正文——思考原文则从页面最底部挪到了**右侧 sticky 面板**。归档现在答得出**是哪台机器、哪个版本产出的**。以及一次全站配色走查把 **25 处不达 AA 变成 0**（其中 15 处集中在 Classic Warm 一套外观里），留下一条规则：一个颜色 token 只能服务一种明暗关系。三个「引用了一个不存在的名字」的 bug——未定义的变量、未定义的 CSS token、未定义的 i18n 键——换来两道新闸门和一次量过之后有意的拒绝。再往前 **v0.4.15**（2026-08-25）把录制存储分了层：原地压实（500 MB → 14.8 MB，33.9x，可逐字节还原）、可搬运的 `.ccwa` 归档、以及导入到 `sources/<标签>/` 独立命名空间。**macOS 升级用户注意**（自 v0.4.2 起未变）：应用包由 `CCWireAnalyzer.app` 改名 `cc-wire-analyzer.app`，`/Applications` 里旧的那份不会被替换，需自行删除。
- **下一步**：
  1. **把自迭代闭环走通**：`/api/diagnose/trends` 现在能可靠回答「新发还是老毛病」，但从「一个复发模式」到「一条体检规则」仍然靠人——`effort_max_rejected_upstream` 之所以存在，是因为有人盯出了那次复发并手写了规则。把这半自动化，雷达与趋势才不只是「读一读」。
  2. **拍板「轮起源该不该在运行时被纠正」**：开发期的真值已经有了——探针把录制与 Claude Code 本地对话记录对起来，量出了启发式的误差（一致率 99.8%、真人轮被降档 0 例，见 `docs/reference/开发约定.md` 第二·六节）。**有意没定的是另一件事**：产物该不该读那些记录来实时纠正自己。现在不读，而这份克制正是要点——这个软件的数据面只有"自己录下的流量 + settings.json 一个字段"，放宽它是信任问题，不是精度问题。同一次测量还带出一个仍然敞着的问题：失败重试会把轮数严重撑虚（某天 2,049 个"轮"里 2,000 个是三个真实问题的 504 重试），分类器该不该合并重试、还是继续如实显示，需要单独论证。
  3. 录制盲区审计（协议面 + 能力面）已于 v0.4.3~v0.4.6 收口，方法见 `docs/reference/开发约定.md` 第二·五节与 `docs/methodology/同类工具构建手册.md` 单元 0。
  4. **压实之后的两件存储后续**，都量过、都有意押后：骨架里的指针列表是同一泳道上一条的前缀扩展，按增量存应该能再砍掉约一半（估算 477 MB → 约 10 MB）；以及保留策略目前只会删，其实可以先压实、把删推迟很久，这会改变「保留 30 天」的代价。两件都不是当前压缩比能用起来的前提，所以没有跟着一起发。

## v0.4.21 - 2026-08-31

- **今天的录制边录边压，不必等到第二天。** `compact_date` 是有意不碰今天的——`append` 只写今天，压实它就要和写盘热路径抢同一个文件——可代价是 28 倍的压缩比在「真正把磁盘填满的那一天」一点用都没有，而实测单日就是 **500~860MB**。现在录到阈值就把已写完的前缀封存成 `{date}.pNN.pack` 分片，跨天再合并回单个 `{date}.pack`。要害那条线一分没让：切段走的是**锁内瞬时改名冻住尾巴 → 锁外逐字节校验 → 锁内就位**，源文件依旧只在校验通过之后才删；压实失败会把这一天还原成切之前的样子。历史天的格式一个字节没变（`PACK_SCHEMA` 不用 bump），归档、导入、跨机搬运照旧。默认关闭，设置页有开关和阈值（20~2000MB，默认 200）。自测实测：两个分片加尾巴合并后 **701KB → 36.8KB（19.1 倍）**，unpack 出来与原始录制逐字节一致。
- **归档一个被切过段的日子，会只剩最后一截而不报错。** `archive_date` 直接压 `{date}.jsonl`，而在新布局下那只是尾巴——偏偏归档默认要删掉原录制。这个是在核对 issue 里一句「顺带确认」时发现的：那句话原本写的正好相反。还原逻辑现在收成一个函数，合并与归档共用，因为「两处各写一份」正是它长出来的原因。
- **给外部 agent 的那份指令，现在把软件已经算好的分析开放了出来。** 端点清单从 3 条扩到 8 条（`trajectory`、`subagents`、`analysis` 此前完全没提），并逐个点名 `trajectory` 里已经算好的字段：`loops`、带 `trigger_step` 的 `subagents`、`phases`、`user_events`、`materials`/`provenance`、`verify`/`valves`、`cost`、`optimal`。不点名等于没开放——agent 不会去猜一个 JSON 里有哪些键，于是它一直在从原文里自己推「哪里在反复」，而答案就在隔壁一个端点里。新增的第二种任务（`?task=flow`）要的是「这一趟实际是怎么跑的」的流程图：它**不规定画法**，只说清这里的「流程图」是什么——一条 agent 自己边跑边长出来的流水线，按三个问题读（状态换了几档 / 物料由什么支撑 / 什么在控制流向）——怎么画交给 agent。指令里还会报出这份快照归纳过没有，免得把机械兜底切出来的 `phases` 当成分析结论。
- **这份指令此前只翻译了一半。** 端点注解和元数据行是写死的中文，于是 `lang=en` 拿到的是「英文任务 + 中文标注」的混合文本。

## v0.4.20 - 2026-08-29

- **一个「AI 归纳」按钮拆成两个，一档一个。** 它此前是**按你正在看哪一档分流**的：同样的字面，
  在列表档跑 `/analysis`、在八视图档跑 `/trajectory`——两件不同的事、两份不同的钱，藏在同一个词后面。
  现在是两个按钮、都常驻，各自显示自己的状态（「归纳列表 / 重跑列表 / 补齐 N 步」对
  「归纳八视图 / 重跑八视图」），于是**不切过去也知道那一档归纳过没有**。同时只允许跑一条：
  两条打的是同一个模型端点，一起跑只会把失败率和限流风险乘起来。「全部重算」也写明了目标，
  并且只给列表——可自定义的归纳提示词只作用于那条管线，轨迹的任务描述是内置的。

- **点八视图那一下，屏幕在同一帧里就有变化了。** 把 iframe 改成背景透明 + 高度跟内容走之后有个
  没料到的后果：子页 load 之前屏幕上一个像素都不动——而 138 节点那条录制要算 5.3 秒、页面 1.2 MB，
  于是用户得到的唯一反馈是「点了没反应」。现在点击的同一帧就渲染出骨架卡（实测 53ms），
  并写明长录制首次打开为什么要等几秒。
- **同一条录制不再每次打开都重算。** `trajectory.compute` 每次 GET 都跑一遍，而录制快照是不可变的；
  现在进程内缓存渲染好的 HTML（容量 2，键 = 快照 + 语义层指纹，语义层跑完即失效），
  138 节点那条重新打开从 **5.18 秒变成 0.03 秒**。

## 更早的版本

v0.4.19 及更早：[CHANGELOG-history.zh.md](CHANGELOG-history.zh.md)，或 [GitHub Releases 页](https://github.com/Fuhehe12/cc-wire-analyzer/releases)（同一份说明）。
