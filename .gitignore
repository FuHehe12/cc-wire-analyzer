# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.uv/

# PyInstaller 打包产物
dist/
build/
*.spec.bak

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# 运行时数据（不入 repo；每台机器运行时自动生成于 ~/.cc-wire-analyzer/）
# 本目录内不应出现这些，但保险忽略
config.json
run.log
port.txt
*.jsonl
*.jsonl.zip

# 本地维护文件（工作区规范，不上传开源仓库）
# CLAUDE.md = 给 AI 的本地维护说明；issues/ = 本地迭代记录（issue 先行）
CLAUDE.md
issues/
