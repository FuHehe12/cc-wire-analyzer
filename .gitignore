# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.uv/

# PyInstaller 打包产物
dist/
build/
*.spec.bak
# 构建时从 tag 生成（见 release.yml / docs/开发指南.md 第九节），不入仓库。
# 注意：.gitignore 的 `#` 只有行首才是注释，行尾注释会成为 pattern 的一部分
# 导致整条规则失配——这一行原本就写成了行尾注释，实测 git check-ignore 不认。
src/_version.py

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# 运行时数据（不入 repo；每台机器运行时自动生成于 ~/.cc-wire-analyzer/）
# 本目录内不应出现这些，但保险忽略
config.json
run.log
port.txt
*.jsonl
*.jsonl.zip

# 本地维护文件（工作区规范，不上传开源仓库）
# CLAUDE.md = 给 AI 的本地维护说明；issues/ = 本地迭代记录（issue 先行）
# ROADMAP.local.md = 从 CLAUDE.md 拆出的路线图构想（260802）——不是承诺，也不该每会话加载
# UI-BACKLOG.local.md = 零散 UI 改进候选池（260808 从 issues/open/ 搬出：它不是 issue，
#   issues/open/ 只放正在推进的改动）；挑到值得做的再单开 issue
# .claude/ = 本地操作规程（workflows/self-audit.md 等，含本机路径与采集姿势）
# sections/ = 项目断面（按版本线做的冻结快照，给人把握全貌用；见 issues/closed/260807_引入项目断面机制…）
# prototypes/ = 可视化原型工作区（260828 从 issues/open/ 搬出：HTML 原型与配套工具不是 issue，
#   issues/open/ 只放正在推进的改动方案；四代原型演进关系见 prototypes/README.md）
# research/ = 轨迹观测研究文档（260828 建：如何观察 agent 轨迹的分析沉淀，
#   方法论属于内部研究，不随开源仓库发布）
CLAUDE.md
ROADMAP.local.md
UI-BACKLOG.local.md
issues/
sections/
promo/
prototypes/
research/
.claude/
