# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.uv/

# PyInstaller 打包产物
dist/
build/
*.spec.bak
# 构建时从 tag 生成（见 release.yml / docs/开发指南.md 第九节），不入仓库。
# 注意：.gitignore 的 `#` 只有行首才是注释，行尾注释会成为 pattern 的一部分
# 导致整条规则失配——这一行原本就写成了行尾注释，实测 git check-ignore 不认。
src/_version.py

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# 运行时数据（不入 repo；每台机器运行时自动生成于 ~/.cc-wire-analyzer/）
# 本目录内不应出现这些，但保险忽略
config.json
run.log
port.txt
*.jsonl
*.jsonl.zip

# 本地维护文件（工作区规范，不上传开源仓库）
# CLAUDE.md = 给 AI 的本地维护说明；issues/ = 本地迭代记录（issue 先行）
# .claude/ = 本地操作规程（workflows/self-audit.md 等，含本机路径与采集姿势）
# research/ = 本地文档的家（260828 建，260829 起归拢全部本地文档，不随开源仓库发布）：
#   轨迹观测/（方法论与判据）/ 项目曝光/（SEO 教程，原 promo/）/
#   路线与待办/（260902 从根目录收进来的 ROADMAP.local.md 与 UI-BACKLOG.local.md）
#   注：prototypes/（十一代可视化原型）已于 260829 收官删除——管线全部吸收进 src/trajectory.py，
#   演进史与脚本对应关系留在 research/轨迹观测/原型演进史.md
#   注：项目断面/（原 sections/，0.4.x 冻结快照）已于 260902 删除，机制记录留在
#   issues/closed/260807_引入项目断面机制与首份0.4.x断面.md
CLAUDE.md
issues/
research/
.claude/
