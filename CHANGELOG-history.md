# 变更历史

这里记录 v0.4.39 及更早版本的变化，最新一版见 [CHANGELOG.md](CHANGELOG.md)。一条一句，只说做了什么；想知道为什么这么定，顺着条目末尾的 issue 名去 `issues/closed/` 读（v0.4.27 及更早的条目大多没有 issue 名，那时还没有这个习惯，查 `git log` 或 `issues/closed/INDEX.md`）。本文件被整理过两次，原文都在 git 里：260913 改写前的那版取 `git show 71b1499~1:CHANGELOG-history.md`，260906 瘦身前那份最长的（含大量解释性正文）取 `git show 9b1673f:public/CHANGELOG-history.zh.md`，英文全文同一提交下的 `public/CHANGELOG-history.md`。历史路径与行为不代表当前状态。

## v0.4.39 - 2026-09-18

### 新增

- 顶栏多了「详情」一格：看某条请求时切去时序或设置，点它就切回刚才那条，之前点出来的翻译和 AI 解读都还在，不用重来一遍。（issue 260918_详情页切走再回来丢失阅读状态）

### 变更

- 捕获详情页里加密思考那段说明收成一句话，不再占两行。（issue 260913_加密思考块与没有思考无法区分）

### 修复

- mac 安装包每次启动都要现场解包并逐个校验几十个组件，双击到界面出现约需 6 秒，改为安装时就放好后，热启动约 0.6 秒、体积不变。（issue 260916_mac冻结态GUI启动慢_onefile解包与逐dylib验签）
- mac 安装包里没有受信任的根证书，点「检查更新」永远报证书校验失败，现在随包带上并用它连 GitHub。（issue 260916_mac冻结态更新检查缺CA证书）
- 极快连续创建快照时列表排序在两条读取路径上不一致，发版自检因此偶发失败，现在排序确定一致、不再偶发。（issue 260915_mac全量验证与索引重建断言偶发红的根因）
- 部分上游会显式声明内容未压缩，此前被误当成解码失败计进错误统计，现在按正常响应对待。（issue 260915_mac全量验证与索引重建断言偶发红的根因）
- 命令行敲了不存在的子命令时，此前一声不吭弹出图形界面把命令挂住，现在报错并退出。（issue 260915_mac全量验证与索引重建断言偶发红的根因）
- 快照对比两段完全相同时不再显示「正文差异」区块，不会再把相同行误读成一处差异。（issue 260915_mac全量验证与索引重建断言偶发红的根因）
- 程序抛出没接住的错误时，终端里不再只剩一个退出码，报错内容会照常打出来。（issue 260913_发版闸门偶发红且不给报错）
- 白板上会凭空多出一模一样的贴纸，批量清理碰到它们还会报删除失败，现在两件事都不再发生。（issue 260915_贴纸重复与批量删除失败与无忙碌反馈）
- 拖动一张贴纸和整理整块白板越来越慢，慢的程度跟着快照总量涨，现在与快照堆了多少无关。（issue 260915_贴纸重复与批量删除失败与无忙碌反馈）
- 备份、拖动、删除、整理、批量清理期间界面毫无动静，现在右下角会转圈说明在做什么、整理到第几张。（issue 260915_贴纸重复与批量删除失败与无忙碌反馈）
- 连点「备份当前录制」会真存下好几份一样的快照，现在一次备份没结束前的重复点击不再重复保存。（issue 260915_贴纸重复与批量删除失败与无忙碌反馈）
- 贴纸位置没存上时界面一声不吭、下次打开又回原处，现在会说明失败并把贴纸放回原位。（issue 260915_贴纸重复与批量删除失败与无忙碌反馈）
- 快照占用把分析结果也数成了快照，张数比白板上多；删快照时留下的旧语义分析文件现在也一并清掉。（issue 260915_贴纸重复与批量删除失败与无忙碌反馈）

## v0.4.38 - 2026-09-13

### 新增

- 捕获页多了「已冷藏」折叠区：很久没点开的日期自动收起来并再压小一截，点一下就解冻回日期栏。（issue 260913_实时分析排版收口与冷藏机制）
- 设置里能关掉自动冷藏或改天数（默认 7 天），也能在捕获页手动冷藏某一天。（issue 260913_实时分析排版收口与冷藏机制）

### 变更

- 实时分析页的字号收成六档，同一层级的内容不再各是各的大小。（issue 260913_实时分析排版收口与冷藏机制）

### 修复

- 实时分析页点开一个目标时，显示的编号与图上那张卡对不上，会把一件任务读成整体目标。（issue 260913_实时分析排版收口与冷藏机制）
- 捕获详情页里被上游加密的思考显示成空白，与模型压根没思考分不开。现在会标明已加密、读不到内容。（issue 260913_加密思考块与没有思考无法区分）

### 文档

- 官网补上实时分析的介绍（新章节、首屏文案与常见问题同步），请求详情一节加实际转发说明。（issue 260913_官网补实时分析与public建站部分下线）
- 建站源码与工具回归主线目录，public/ 冻结快照里对应的副本下线，原始摘要保留可核验恢复。（issue 260913_官网补实时分析与public建站部分下线）
- 披露断面开始分批下线：先删与主线逐字节一致的许可证副本和只差两处链接的构建手册，其余待内容复核。（issue 260913_披露断面分批下线）
- 变更记录改成三份：日常看的两份只用中文，发版给 GitHub 用的三语正文单独放一份，发版前对不上会直接挡住。（issue 260913_CHANGELOG分层收口与三语只走发版）
- 变更记录的写法要求收归开发入口文件一处，开发约定里那份互相打架的旧口径删掉。（issue 260913_CHANGELOG分层收口与三语只走发版）
- 变更记录顶部的状态卡从五节收成三节，不再抄当前版本的功能描述。（issue 260913_CHANGELOG分层收口与三语只走发版）

## v0.4.37 - 2026-09-13

### 新增

- 捕获详情页显示每条请求实际发给了哪个上游，悬停看完整地址。混用官方和第三方上游时，能看出某次失败是谁拒的。（issue 260913_捕获详情显示实际转发上游）

### 变更

- 实时分析页的空白说明改成先讲你会遇到什么问题，再讲这一页怎么帮你。（issue 260908-260912_实时分析页面与动作账本）

### 修复

- 说明书生成脚本在 Python 3.11 下无法运行。（issue 260713-260912_发版链路与产物一致性）

### 文档

- CHANGELOG 的写作要求移进 CLAUDE.md，条目改成短句。（issue 260714-260912_CHANGELOG写法的反复）
- 自测清单补回一项漏登的检查，它此前只在 CI 上跑。（issue 260809-260912_文档体检与闸门机械化）
- 架构总览里的接口清单补上漏掉的四十多个接口，并加了自动检查防止再漏。（issue 260809-260912_文档体检与闸门机械化）

## v0.4.36 - 2026-09-12

### 文档

- 观察者提示词写清了「最高目标」怎么判、什么样的变化才算换了目标。此前目标被抬高或收窄容易被记成普通调整。（issue 260909-260912_观察者提示词的判据演进）

## v0.4.35 - 2026-09-12

### 新增

- 看历史日期时，今天新来的录制会在日期条上标出待看数量，点一下切过去。此前它们会被静默丢掉，只能自己想起来手动切。（issue 260713-260912_前端可用性与刷新缺陷）

### 变更

- A→G 图把整体目标和它下面的任务分层画出来，此前两者一眼看不出区别。（issue 260908-260912_实时分析页面与动作账本）
- A→G 图的卡片不再互相压住，间距也排齐了。（同一 issue）
- 顶部「当前的理解与现状」重新排版：宽屏不再只占半边，要点分行显示。（同一 issue）
- 「录制分析agent行为」补上中英文之间的空格。
- 修正两处浏览器检查脚本的判断条件：一处改成忽略空白比对，一处收窄命中范围。（同一 issue）

### 修复

- 时序页的数量色块在三套外观下都看不清，改成自动压暗底色直到白字够清楚。（issue 260730-260912_视觉系统与走查）

### 文档

- 观察者提示词补上「整体目标变了」的判据和例子。此前用户连着换要求时，观察者会把一串跳变压成一个目标。（issue 260909-260912_观察者提示词的判据演进）
- 写清楚哪些「用户消息」其实来自另一个 Claude 会话而不是真人。实测某场会话的 46 条里有 28 条是这类。（同一 issue）

## v0.4.34 - 2026-09-11

### 新增

- 盲区雷达新增工具面：报告没见过的内置工具，以及同一条对话里工具集中途变了。（issue 260802-260911_盲区雷达与未知检测）
- 未知检测里的能力声明段落带上来源主机和 CC 版本，此前只有它没有归属信息。（同一 issue）
- 访问不存在的接口时会提示最接近的那个，并指向接口说明。
- 检索接口说明哪些地方不在检索范围内——查能力声明和工具面要走另一个接口。

### 变更

- 已知能力声明列表补进一个新出现的，它的含义正是「工具集可以在对话中途变化」。
- 索引格式升级，升级后首次读取会重建当天索引。
- 样例数据脚本换用真实工具名，免得样例自己制造噪声；并补上「跑完立刻删」的警告。

## v0.4.33 - 2026-09-10

### 变更

- 取消八视图分析。真机用过之后的结论是静态的事后切分没有用，看一段录制的需求由轮次骨架总结和目标流满足。轮次骨架总结一字不动。（issue 260828-260910_八视图的立与废）

## v0.4.32 - 2026-09-09

实时分析分成两层看：整体目标在外、任务在内；做错的目标不再被复盘吸收；顶部两段能收起来。

### 新增

- 观察 AI 可以按对话轮读录制，一轮是一次用户消息加它引发的全部步，不在轮中间截断。实测 54 步的对话首次读取从 144 万字符降到 34.7 万。
- 用户对选项提问的答复不再消失，标成「答复」留在原位。
- 账本区分消息来源：真人、斜杠命令、系统合成、上级 AI 派给子代理的指令。此前一律显示为「用户」，观察者会把内部合成的伪轮当成真人要求。
- 导出页改成两层：整体目标在外，任务在内。此前一场只有一个目标的会话看上去像换了十几次目标。
- 目标流可以给做错的目标留痕：曾经相信、后来判定方向本身就错了的目标，标红划掉留在序列里。
- 顶部两段说明可以折起来，收起后留一行摘要，这块高度降到原来的六分之一。
- 观测可以带一句口头汇报，最长 120 字符——一句话装不下清单，写的人只能挑最重要的说。
- 目标流可以声明自己是现场逐轮跟随建的还是事后一次性复盘建的，复盘模式下页面会明说「中途被推翻的目标可能没有留痕」。

### 变更

- 接口响应显式声明编码。这不是 Windows 乱码的解药——乱码出在客户端控制台，真正的解法写在接入说明里。

### 修复

- 目标图的卡片不再互相压住，连线也不再交叉。
- 各视图顶栏统一成同一种卡片和字号，此前三处长相都不一样。

### 文档

- 契约写清整体目标与任务是两个平面、做错的目标与「被接替」的区别，以及怎么给旧观测补挂目标流。
- 接入说明改为按轮读、逐轮建模。
- 接口契约补上按轮读取的参数与返回字段、问答例外和来源前缀的判据。

## v0.4.31 - 2026-09-09

捕获与实时分析之间可以互相跳，证据能回到原文。

### 新增

- 捕获详情可以直接打开覆盖这条请求的实时分析；还没有观测时，带着这段对话的范围过去。
- 证据里的请求编号可以点回它在捕获里的完整正文。
- 观察 AI 可以整体重建已有的 A→G，必须写明理由，上一版保留可查。

### 变更

- 三列按可用宽度展开，中文行宽不再按字母宽度截断。
- 窄屏顶栏改成两行，标题不再被页签挤成竖排。

### 修复

- 滚轮停在目标流画布上时页面无法滚动。
- 详情里的列表与引用比正文大一号，强调改用色条和字重。
- 观测下拉按日期排列，没写标题的用当前理解回落成一句摘要，不再是一整列「未命名观测」。

### 文档

- 接入说明与界面指引补充重建与跳转的用法。

## v0.4.30 - 2026-09-09

实时分析聚焦三件事：AI 怎样理解你的要求、怎样判断现状、目标怎样修正或转向。

### 新增

- 顶部新增「它对你要求的理解」和「它对现状的判断」，先看清双方是不是想的一回事。
- A→G 目标流保留最初的理解，并区分同一任务里的修正和转去另一件事。
- 目标状态、核验和观察者的解释订正分别留痕。AI 自称完成不等于通过验收。

### 变更

- 主画面集中呈现 A→G，收起「工作与发现」等三块；旧记录与接口保留。
- 目标详情就地展开，原话证据就地查看；刷新保留选择与阅读位置。
- 观察 AI 可以增量更新，不必反复提交全部历史。

### 修复

- 观测写入遇到错字段或内容超限会明确报错，不再出现成功回执却没生效。
- 账本续读到末尾后不再重复读历史。

### 文档

- 三语接入说明与界面指引更新，并写明刷新页面不会自动启动观察 AI。

## v0.4.29 - 2026-09-08

### 新增

- 读取接口新增对话视图：只输出用户说的话、AI 的回答和思考，工具调用压成每步一行。实测 311 步的对话从 6.0 MB 降到 222 KB。
- 实时分析改为目标—阶段—产物的关系图，阶段可以下钻看动作和证据。
- 观测条目可以用首末请求编号声明一段连续阶段，不必逐个枚举成员。
- 预测记下依据与判断条件并封存原文，支持与反证单独核对，不把观察者猜错等同执行者出错。
- 新增只读的增量投影接口：工具返回按同一泳道里先前的调用编号配对，区分缺失、空结果和编号重复。

### 修复

- 同一段 AI 输出此前会显示两次，现在只在产生处出现一次。
- 快速切换观测时，旧响应不再覆盖新内容。
- 接入说明补齐没有观测时的创建流程；修正窄屏溢出和浅色外观下的文字对比度。

### 文档

- 补充状态协议、范围展开与增量读取的回归测试并接入 CI；浏览器回归脚本覆盖目标阶段图、证据下钻、三语三外观和三档宽度。
- 用本机复制的 54 请求会话验证语义阶段与预测回放，用 247 请求 18 轮验证旧观测压缩。真实录制与截图只留在本地，不随代码发布。

## v0.4.28 - 2026-09-08

给「外环观测者」开通读写——外环是另开的一个 AI，跟踪被观测的 agent 做了什么、在做什么、接下来要做什么。

### 新增

- 新增读取接口，把一条录制还原成去重后的会话全文，54 步的主线从 19.8 MB 降到 241 KB。
- 新增观测读写接口，重复提交不会重复生效，整批写入，改判留痕。
- 新增「实时分析」标签页，观测清单按类分组，证据可以点回具体请求。
- 详情页加了泳道内的前后导航和时序图定位。
- 说明书加入「三类文档与协作」一章。

### 变更

- 恢复发版流程；`public/` 仍然冻结，发版不更新它。
- 产品资料可以独立生成 HTML 说明书。
- 测试与工具按职责分目录。

### 文档

- 接口契约补两个新端点，自测清单增至十三条。
- 文档检查增加具名参考文档的正文检查。
- 界面导览补详情页的泳道导航；《报文解读》新增「拿网上流传的提示词对照录制」一节。
- README 改成给人读的入口；变更记录统一以中文为准。

## v0.4.27 - 2026-09-06

### 变更

- 默认启用滚动压实，预填 DeepSeek 并提高分析上限；API 密钥保持为空。

### 文档

- 官网补充提示词、运行时序、快照和 AI 解读的说明，提供在线文档与可放大的截图。
- 文档维护规则并入产品说明书。
- 接口契约与开发约定一度迁进说明书，同日改回 Markdown 作为维护源。

## v0.4.26 - 2026-09-05

### 新增

- 详情页新增调用参数折叠区，请求体里三大块之外的字段原样摆出，没见过的键标橙。

## v0.4.25 - 2026-09-04

### 修复

- 变更记录加了长度闸门：一条不超过 25 词或 40 字，不许硬折行。
- 文档里的引用块只留警示与自检钩子，69 处修辞性引用降为正文。
- 同类工具构建手册与《报文解读》各自搬到新目录，并检查相对链接可达。
- 文档检查开始对账接口标题：同一个接口只准占一节，声明的参数必须真实存在。
- 上游拒绝后原样重发的请求并入它重试的那一轮，标「重试 ×N」，不再各开一轮。
- 思考档位改按有没有明文判，只回签名的录制降为 C 档。
- 接口契约删掉过时的复制品，六份文档去掉文章式引子。

## v0.4.24 - 2026-09-02

### 修复

- 审查子代理那次调用的安全审查，归到那个子代理，不再算在主线上。
- Windows 上索引格式变更后不删旧文件、只往后追加，索引越堆越大；现在真的替换，已经膨胀的文件首次读取时自动清理。
- 时序图里一张折叠卡代表多个节点时，不再画出几十条重合的连线。
- 辅助泳道不再把分属不同会话的调用串成一条线。
- 五份参考文档全量体检，自相矛盾和互相矛盾处逐条改对。

## v0.4.23 - 2026-09-02

### 新增

- 识别 Claude Code 在用户离开后判断要不要通知的调用，此前归为「其他」。

### 修复

- 建议补全、离开回顾和内部检索派发改归辅助，不再计入主线轮数；后台任务通知仍算真轮。
- 辅助泳道的两个标签补齐中文和日文。

### 文档

- 《报文解读》的分类顺序与代码对齐。

## v0.4.22 - 2026-09-01

### 新增

- 盲区雷达列出那些被判为主线、却缺少主线结构特征的请求。

### 变更

- 索引格式升级，旧索引自动重建。

### 修复

- 会话标题与短链请求重新归为辅助，不再计入主线。
- 长得像对话但不带工具清单的请求不再判为主线。
- 工具结果附带的图片说明、网页正文和打断标记不再另开一轮。
- 三份轮边界实现合并成一份，时序视图与分析视图共用同一判据。
- 兼容 Claude Code 2.1.238 起的安全审查格式，恢复待判定动作的显示。
- 时序图的安全节点补上判定结果，除待审动作外也能看到是否放行。
- 对账探针补齐压实后录制的读取，避免它们静默脱离对账。
- 对账探针增加三档模式，分别对账归属、轮数和轮起源。

## v0.4.21 - 2026-08-31

### 新增

- 当天的录制支持滚动压实：写完的分片先封存，跨天合并成一个包。默认关闭，阈值可设 20～2000 MB，解包能逐字节还原。
- 给外部 agent 的分析指令从 3 条扩到 8 条，并增加流程图任务。

### 修复

- 分片录制归档时只保留了最后一段，现在归档覆盖完整录制。
- 英文版的 agent 指令不再混有中文标注。

## v0.4.20 - 2026-08-29

### 变更

- 列表与八视图各有独立的归纳按钮，两条管线不能同时跑。
- 八视图结果按内容缓存，重复打开不再重算。

### 修复

- 八视图加载时立即显示骨架和等待说明，不再是一片空白。

## v0.4.19 - 2026-08-29

### 新增

- 分析页的树视图换成轨迹八视图，列表保留。用全部主线请求补回压缩前的历史；事实由程序算，语义由模型写。八种读法涵盖状态快照、物料血统、验证矩阵、阀门回路、能耗方差、最优轨迹表、物料生命线和最优时序图。

### 变更

- 八视图接入主界面的三套外观和打包字体，小字加大；父子页协商高度，不再出现双滚动条。切换外观原地重画，不重算录制。

### 修复

- 修正在途时长单位、阶段耗时缺失和取整不一致；补齐图例、空泳道说明与长物料名的排版。
- 生成不了轨迹时返回同款外观的错误页，不再把整页接口文字当成错误提示。
- 可得性横幅区分「有思考块」和「没有明文」，不再显示「有思考但共 0 字」这种自相矛盾的说明。

## v0.4.18 - 2026-08-28

### 变更

- 步级简报改成标题、原因、结果三段：标题最多 20 字，只在真有权衡时写原因，结果里的短引必须能在工具结果里找到。旧格式继续兼容。
- 轮头区分用户要求、本轮任务、完成条件、结果与风险；完成条件只引用用户说过的标准。

### 修复

- 步级归纳补入用户指令、动作、作用对象和工具结果摘要；纯执行步并入前一个判断步，不额外调用模型。
- 补齐命令作用对象的解析，支持先切目录再执行、带引号的绝对路径等写法。真实录制上 199 步全覆盖。

## v0.4.17 - 2026-08-27

### 新增

- 归纳按阶段落盘并记下失败的步号，重跑默认只补缺，另设「全部重算」。
- 快照、AI 归纳与对话历史可以导出成便携包。导入时编号冲突会另建一个，不覆盖本地内容，并保留机器来源。
- 接口浏览面给需要参数的端点提供可编辑的完整地址，优先用本机真实录制生成样例；没有数据时明确标为参考格式。

### 变更

- 归纳顺序改成先写步级简报再汇总轮级结论，避免骨架截断丢步。批次默认并发 4，可设 1～8。
- 思考抽屉的宽度可以拖动，偏好只存在浏览器里；窄屏仍然移到正文下方。

### 修复

- 自动更新重启时清掉打包器留下的临时环境变量，避免新版本复用旧解压目录、加载旧界面。
- 思考抽屉按实际位置算可用高度，只保留一个滚动容器；打开时定位到吸顶区，翻译结果不再落在屏幕外。
- 快照索引按编号格式识别文件，不再把分析侧车误收成快照。
- 修正经典暖灰外观下贴纸标签的对比度。
- 接口浏览面补齐三套外观，修正实验室日光误用暖灰色板的问题。

## v0.4.16 - 2026-08-27

### 新增

- 录制分析接回子代理请求，按派生步骤展示折叠简报和单步思考，支持三层嵌套。匹配不上或录制不可用时单列原因，不说成「没有子代理」。
- 设置页的「给 AI 用」卡增加接口浏览面入口，地址可点可复制。
- 列表增加步级简报、工具聚簇与机械信号，结果随分析缓存；轮级和步级提示词开放设置。

### 变更

- 步级简报拆成最多 24 字的标题加细节；思考原文移到右侧吸顶抽屉，Esc 关闭。
- 子代理的边线、徽章与名称统一为蓝色，三套外观各自校正文字对比度。
- 录制分析改成单选，提示词仍可双选对比。
- 多源指令清单默认折叠，标题显示条目数。

### 修复

- 修正三套外观的文字对比度，把装饰色、正文色、危险按钮和贴纸角色文字色拆开；新增对比度探针，展开态复测通过。
- 子代理卡头与工具行明确字号，不再继承卡片的大字号。
- 工具聚簇行与步骤行共用列宽和字号，长步骤区间不再挤压工具标签。
- 工具名标签补上浅色背景，修复浅色外观下白字看不见。
- 重复的工具调用合并显示。
- 补齐分析提示词的翻译键；检查工具同时验证界面引用的键真实存在，避免三张语言表同时漏键还能通过。
- 修复提示词对比：删掉它对一个已移除参数的引用。
- 压实包与归档写入机器名和真实版本，导入后保留来源；旧归档缺少这些信息时显示未知。
- 来源列表区分本机与外来数据。
- 从导入来源备份录制时完整传递来源，不再跑到本机命名空间里找而返回 404。
- 白板、贴纸、信号、树分叉等一族改用外观变量，补齐两套浅色的显示。
- 文档检查会拦截引用了但没定义、也没有兜底值的样式变量。

## v0.4.15 - 2026-08-25

### 新增

- 翻译、解读与分析对话的输入上限改成可设的字符数，默认 20000，可设 1000～2000000；截断提示指向对应设置。
- 新增只读浏览面，把接口返回的 JSON 或说明书渲染成人能读的页面；不加参数时响应逐字节不变。
- 录制支持按内容压实、随机读取和单文件归档。完整重建逐字节校验通过才移除原件，可以还原。
- 外来录制导入到独立目录，所有读取面都支持指定来源，界面明确区分本机和外来数据。

### 变更

- 区分压实、归档与清除：压实可逆，归档默认保留原录制，只有清除才真删。这一版只压实过去的日期，不动正在写的当天。

### 修复

- 工具调用的输入支持折叠与全文展开，去掉搜索结果的条数截断；保留的行截断显式标出。
- 空的日期参数按未指定处理；保留天数的自测改用相对日期，不会随时间失效。

## v0.4.14 - 2026-08-10

### 新增

- 轮起源用本机日志核对过真值，并记录了两侧请求编号的对应关系。这个对应只适用于提供该字段的 Anthropic 上游，网关的日志编号不能替代。
- 上线中英双语落地页，补齐搜索与分享元数据；三语 README 增加产品全称和下载入口。

### 文档

- 开发约定增加七层识别地图，写明官方标识与启发式回退的关系；轮起源未知时回退成「用户」。

## v0.4.13 - 2026-08-09

### 修复

- 更新下载会占用运行状态并立即给出反馈，重复请求接回当前进度，不再重复下载。v0.4.11 的旧界面仍带这个毛病，升级时只点一次并等待，或手动下载替换。

## v0.4.12 - 2026-08-09

### 新增

- 时序图区分用户轮、系统合成轮、命令轮和残轮，自动轮单独显示，不隐藏实际工作和成本。辅助调用按轮聚合并可单独折叠。
- 设置页实时列出运行中实例的端口、模式和录制状态。探测固定在 5051～5100，绕过系统代理，不依赖可能过期的进程号或端口文件。
- 轮次骨架增加可重跑、可缓存的 AI 语义归纳，事实仍由程序抽取。模型引用的步号要过白名单校验，无效的剔除并记下来。
- 设置页按录制、索引、存档、快照和日志显示磁盘占用，只看文件大小，不读内容、不加删除入口。

### 修复

- 残轮的折叠入口挂到第一个可见成员，展开后可以单独收回。
- 贴纸的角色徽章移开删除按钮，拖拽时排除删除和确认控件，避免点击前重画导致按钮失效。
- 新快照落位时检查已有贴纸的位置，跳过被占的格子。
- 文档检查给外部工具的端点加具名白名单，不再豁免整篇手册。

## v0.4.11 - 2026-08-09

### 新增

- 更新流程支持下载、校验、替换和重启，每一步都由用户触发，不定时检查也不静默安装。录制中拒绝替换；Windows 替换失败整体回滚，macOS 只下载校验并定位文件。有校验和时必须比对，没有时明确提示。
- 版本号同时写进接口、Windows 文件属性、macOS 应用信息和产物文件名；发布产物附带校验和清单。
- 打包脚本统一本地与 CI 的版本、命名和校验和。

### 修复

- 切换语言时从缓存重画快照对比，不重新请求接口。
- 快照对比的可比性提示补齐三语，缺键才回退后端原文。
- 更新重启的顺序改成先恢复配置、再启动新进程、最后退出，避免旧进程撤销新进程的接管。
- 两份打包配置补齐六个依赖的运行元数据，修复本地打包后启动服务报找不到包。

## v0.4.10 - 2026-08-08

### 新增

- 分析页提供提示词与录制快照看板，支持拖拽、从详情或选中文本保存、单录制分析和双快照对比。快照以单条请求保存当时的上下文，内容不随后续录制改变。
- 快照支持内置模型多轮分析，逐字稿落盘并可经接口读取。问题按可得性切换，看不到明文时禁止臆测心理。
- 快照可按类型、标签和日期批量清理，先预览再确认；保留天数不会自动删除你手动存的快照。
- 内置模型可以解释和翻译快照差异，只发送差异本身，不同时发两份全文。
- 差异比较先显化零宽字符、空白与换行的差异再比文本，同形异码字按字符标出，避免把普通改动误报成隐藏水印。
- 不可见字符的差异带行号定位和高亮。
- 步骤骨架增加树视图，展示命中句、实际工具与分支等机械信号；页面明确这些是候选线索，不是模型真实的决策树。
- 思考抽取分骨架、摘录、全文三档，按信号密度分配预算，并如实返回是否超预算。
- 思考可得性按步骤分三档：能看到明文、只能看行为链、整块加密不可读。
- 列出 system、消息注入和工具描述等指令来源，重复注入合并计数。
- 复制给外部 agent 的是本机端口、端点和按可得性调整过的任务，不直接复制整份录制。
- 记录最近 5 套真实上游配置并提供一键还原，本机代理地址不进历史。还原覆盖整个环境变量命名空间，凭据和模型映射一并恢复。
- 新增引用检查工具，核对界面脚本里的调用与样式类引用。
- 新增快照自测，覆盖预算、行为链与隐蔽差异的正反例。

### 变更

- 文档检查按差异性质返回结果：事实错误挡住构建，没登记的内部能力只提示。
- 文档检查核对打包配置里的资源路径和双平台共享配置，防止说明书迁移时漏打包。
- 文档检查对账请求类别、错误类别与体检规则编号，按各自语境查不存在的枚举值。
- 代理地址异常的警告提供直达上游配置历史的修复入口，区分残留污染和合法的本地网关。
- 捕获列表宽度上限放宽到 1760px，时序图按视口放宽到最多 2560px 并保留边距。

### 修复

- 上游还原成功后同步刷新地址警告；体检建议直接导向历史还原，不再建议那些不会成功的重启。
- 接管配置失败时仍保留服务和修复接口，并在日志里给出恢复命令，此时不录制。
- 没在录制时，设置页从配置文件读当前地址，不再显示用于恢复的内存快照。
- 上游错误体按声明的字符集解码，没声明时依次尝试几种编码并记录回退，不再用替换字符损坏原文。
- 失败指纹归一化会把长十六进制的请求编号也算进去，避免同一次限流碎成很多组。
- 四个静态检查脚本统一输出编码，避免 Windows 控制台乱码。
- 修复提示词看板交换两侧时抛异常。
- 文档检查递归扫描子目录，读不到具名依赖直接报错，不再把读不到当成没问题。
- 服务与代理启动接口共用同一段录制启动逻辑，补齐上游配置历史的采集。

### 文档

- `docs/` 拆成参考手册、方法材料和索引，检查按目录覆盖，不再维护单独的文件清单。
- 文档维护策略从按日期堆叠的腐化清单，改成按问题归纳的教训表。
- 开发指南改名开发约定，问题域手册改名同类工具构建手册，名字与用途对上。
- 架构总览聚焦模块分层与数据流，历史和设计取舍分别指向对应记录。
- 界面导览移除已经解决的反思项，保留仍然有效的限制与改进问题。

## v0.4.9 - 2026-08-03（紧急修复）

### 新增

- 新增静态检查，核对卡片行数、内边距和高度常量，并用反例自测检验判据。

### 修复

- 加高辅助聚合卡，恢复被裁掉的分类计数徽章。
- 命令行的失败统计返回成功标记，不再被误判成命令执行失败。

## v0.4.8 - 2026-08-02（紧急修复）

### 修复

- 折叠时序图时恢复辅助泳道，每条主线显示一张辅助聚合卡。

## v0.4.7 - 2026-08-02

### 新增

- 增加深色专业、经典暖灰和实验室日光三套外观，默认深色；偏好只存浏览器，不进配置。补齐键盘、无障碍、减弱动画、窄屏与缩放支持。
- 未知检测接口聚合没见过的块、字段与枚举，返回样例和来源信息，便于从真实录制发现协议盲区。
- 新增跨天失败趋势，提供每日曲线及供应商、模型、CC 版本切片；只有接口和命令行，不进界面。
- 新增检索与统计接口，与命令行共用同一份存储层实现，避免口径分叉。
- 命令行增加未知检测和趋势两个子命令，只读分析面统一支持按会话过滤。
- 新增文档对账工具，核对端点、命令、路径、索引版本和自测清单；这一版只报差异，不挡构建。
- 索引记录上游主机和 CC 版本，供应商按实际路由的主机判断，不靠模型名猜。
- 增加配额嗅探与 hook 评估两个分类。
- 专门渲染服务端工具调用，索引记录输出格式；启动时提醒本机代理地址，合法的其他本地网关不拒绝。

### 变更

- 时序图按对话轮折叠，以用户话语、子代理和辅助计数为入口；只有整轮失败才把整卡标红。
- 统计改读索引，不再逐条读正文并重复分类。
- 时序图按日期和录制大小缓存；多会话列表显示会话短码。
- 索引增加轮首的用户文本，避免提醒注入超长后丢失用户原话；捕获摘要增加会话编号。

### 修复

- 适应宽度的缩放下限设为 50%，避免多泳道图缩到文字不可读。
- 盲区结果补充主机与 CC 版本归属；录制降级单列一类，不再混进协议演进。
- 趋势区分形态和新鲜度；空泛的错误按主机拆分，避免跨供应商误归成一组。
- 补齐上下文压缩的已知集合与渲染，不再把自己组装的块当成未知。

### 文档

- 补齐检索、统计、雷达字段与三外观的开发约定，盲区观测方法归入同类工具手册。
- 三语 README 的 12 张截图按新外观重制，用的是合成数据，不含真实录制。

## v0.4.6 - 2026-08-01

### 新增

- 检查面支持按会话包含或排除，按前缀匹配，在分页前过滤，保证总数真实。
- 日期数据加载时显示徽标，避免分不清是在加载还是真的空。

### 变更

- 检索范围增加系统消息、工具结果、工具调用和工具定义，并返回实测覆盖率；零命中时附说明。

### 修复

- 统计补计缓存写入的 token，修复用量汇总缺项。
- 按编号取记录时自动排除索引文件，避免命中残缺的索引行。
- 解压失败的记录进入索引并单列一类，不再算成功；它与上游拒绝区分开，同时出现时以上游错误为准。
- 协议扩展基线兼容改名前的旧值，不再把老录制误报成未知。

## v0.4.5 - 2026-08-01

### 变更

- 辅助请求优先按会话挂到所属主线，只在找不到会话时才回退到时间邻近。
- 子代理泳道优先用官方的代理编号，旧录制回退到从提示词派生的键。
- 隐藏主线或子代理泳道时，级联隐藏它关联的节点，恢复时保持同样的关系。
- 子代理用派生主线的同色淡化版，强化父子关系的辨识。

### 修复

- 没有配置文件时按空配置读取并跳过备份，接管时创建最小配置，修复新机器上服务启动即退。
- 复制给 agent 的指令随界面语言生成，不再固定中文。
- 翻译和解读区分输入截断、输出上限与内容审查，并显示上游的真实原因。
- 给可见文本补齐选取与复制，不再只能看不能带走。
- 快速切换日期时丢弃过期的请求结果，避免图表被先前日期的响应覆盖。

## v0.4.4 - 2026-08-01

### 新增

- 设置页增加 90%～200% 的界面缩放。

### 变更

- 失败聚合改成捕获页里可展开的摘要入口，不再长期占据首屏横幅。

### 修复

- 跨午夜后刷新日期与数据订阅，软件不再继续显示昨天的空列表。
- 提高次级文字的对比度，修复低于无障碍标准的小字颜色。

## v0.4.3 - 2026-08-01

### 新增

- 捕获页增加失败聚合入口，可以展开错误组、查看请求字段和样例，不再只能经接口查询。
- AI 使用说明随软件打包，接口会附上实际端口和路径，命令行加 `--help` 可打印说明；设置页提供给 agent 的复制指令。
- 详情页展示服务端工具调用和服务档位。
- 详情显示协议扩展及未知提示，索引补记上下文管理、诊断、停止序列与思考预算。
- 聚合上下文压缩的增量并记录停止序列。
- 聚合思考签名与引用信息，不再丢失。
- 记录官方的代理编号作为子代理交叉校验位，但不据此更改既有的分类结论。

### 变更

- 协议扩展区默认折叠，摘要保留数量与未知提示。
- 响应头恢复默认折叠，保留链路专有字段的突出显示。

### 修复

- 纠正「没有控制台就没有标准输出」这个错误假设，恢复管道与重定向下的帮助输出。
- 捕获列表按失败标记显示状态，HTTP 200 但流内出错的不再显示成功。
- 详情页按请求本身判断是不是流式，不再把普通响应的分块数当成流式事件数。
- 工具调用轮的捕获摘要显示实际的工具动作，不再留空。
- 切换语言时重画体检与失败聚合条目，不再保留旧语言。
- macOS 打包配置补齐 Windows 已有的解压依赖，防止非流式响应解析回归。
- 能力面录制审计确认了核心解析；Workflow 派生的子代理用代理编号分开泳道，动态脚本的派生关系仍是已知限制。
- 识别流式错误事件并计入失败统计；此时 HTTP 状态可能仍是 200，调用方必须检查错误字段。
- 解压与解码失败保留诊断标记和可恢复信息，不再静默丢掉响应正文。
- 增加两种压缩格式的解压依赖，覆盖 CC 声明的全部格式。
- 应用版本从发布标签派生，避免界面自报的版本与产物不一致。

### 文档

- 修正恢复路径、错误枚举、索引版本、字段长度和固定端口等文档漂移。

## v0.4.2 - 2026-07-30

### 变更

- 两个平台的产物统一命名；macOS 应用改名，旧名的应用不会被覆盖，升级时需要自行移除。

### 修复

- 详情页请求侧的元信息改成卡片，与响应侧对齐。
- 捕获列表改用固定列宽，模型名和类别标签不再推动其他列；列宽兼顾三语，长值截断后可看全文。
- 时序图图例换行后左对齐，不再保留宽屏推到右边的偏移。
- 设置页按钮禁止折行，标签列保留最小宽度，长路径在值区域换行。
- 时序图的安全节点显示待审动作，与列表共用同一套摘要格式。

## v0.4.1 - 2026-07-29

### 新增

- 安全审查展示待执行的工具与参数、严重度或放行判定、规则理由和发送内容；未得到判定时明确提示，不显示成空。
- 关于页增加检查更新，12 秒超时后提供手动入口。
- 详情页按内容为 system 块标注计费头、身份、审查等角色，帮助辨认大段文本的用途。

### 变更

- 捕获列表为非主线请求显示类别标签。
- 备份份数从捕获状态卡移到设置页。
- 列表与详情页的首字时间标签支持中英日三语。
- 请求侧的思考块接入统一文本工具条，支持翻译和 AI 解读。
- 移除按官方定价推算的金额，避免误导第三方网关用户；保留上游原始用量。

### 修复

- 详情页把请求体的模型和流式标记放回请求侧，响应区只留响应字段。
- 隐藏主线时同时隐藏关联的辅助调用，空的辅助列不再占位。
- 删掉命中不了的 system 角色标注和 9 个死翻译键。
- 删掉代理启动响应里恒为空的字段，保留实际在用的状态字段。

### 文档

- 新增《报文解读》，说明七类请求、报文形态、判别方法和易混点。
- 开发约定集中到一份文件，架构与贡献指南改为引用；补上「模板改完要重启服务」。
- 三语 README 改从使用场景、真实故障案例和数据边界介绍产品；只需要普通历史记录的人，建议直接读 Claude Code 日志。
- README 移除手填版本号，改用发布列表和日志指针。
- AI 使用说明改为中文，英文与日文 README 标明深度文档的语言。

## v0.4.0 - 2026-07-28

### 新增

- 失败聚合按归一化后的错误消息分组，同时返回请求字段、类别和样例。输出有上限并报告截断；只整理证据，不调用模型。
- 配置体检检查上游、凭据与档位冲突，界面和接口共用同一份结果；启动遇到错误级问题会拒绝，可以显式强制越过。体检只读、不自动修复。

### 变更

- 停止原因与错误类型改成三语标签，响应头默认展开并突出链路独有字段。
- 体检返回的结果说明它读的是配置文件，运行中的会话可能仍持有启动时的环境；不跨进程读环境来弥补这个盲区。
- 主线泳道显示真实会话短码，可查看完整值；子代理仍显示派生实例码。
- 新增泳道探针，展示官方标识并与分类器双向核对，不一致就告警，用作后续 CC 版本的回归探针。

### 修复

- 并行的同模板子代理不再挤到一条泳道上：探测前缀从 120 字加到 300，匹配长度从 200 加到 1000。前 300 字仍然相同的边界情况留待后续。
- 子代理优先用官方标记判断，未知形状回退主线；派生匹配先去掉提醒再做对齐。
- 主线按官方会话编号分组，缺失时才回退文本键。
- 子代理按派生实例归并，不再每个请求独占一条泳道。
- 自测从 5150 起选空端口并用接口探活，避免固定端口被占时误测到真实实例。
- 索引记录增加格式校验，版本不符先删除再重建，避免旧索引静默缺字段。
- 自测样例补齐真实 system 块、会话头、用户元数据与子代理提醒。

### 文档

- 新增界面导览、架构总览与文档维护策略。
- CLAUDE.md 按速览、背景和开发约定重组，归纳反复出现的故障与子代理判据。
- 变更记录顶部增加项目速览，给出定位、状态和下一步。
- 接口文档对齐体检、失败聚合、流式翻译与解读、用量字段和代理状态。
- 中文变更记录改成书面表达，保留事实与技术标识。

## v0.3.2 - 2026-07-19

### 新增

- 同一泳道连续两次以上的错误可以折叠成计数卡，显示时间范围和摘要。
- 工具栏增加泳道选择器，可以逐条控制显隐，切换日期时重置。

### 修复

- 增加写入时的轻量索引，列表和时序图不再反复读完整录制，也不再限制为 1000 条。
- 实时模式改用增量更新，只有日期、过滤或档位变化才整体重建。

## v0.3.1 - 2026-07-18

### 修复

- 阻止把代理自己当成上游造成转发死循环：启动时按回环地址和自身端口检查，已经被污染的恢复记录不再写回自指地址，转发路径也加了兜底。

## v0.3.0 - 2026-07-17

### 新增

- 时序节点分用户消息轮、工具循环细条和纯对话轮三档，不靠语义猜测；错误节点不降档。
- 每 2 秒检测配置文件被外部修改，发现绕过代理时显示断开，不回写用户的新配置，并提供重新接管。
- 运行日志记录启动、关窗、接口停止、收尾和信号等各种退出路径。

### 变更

- 发布说明改从对应的变更记录版本段提取，缺失时回退到标签信息。

### 修复

- 运行日志改用 UTF-8，避免中文 Windows 下乱码；历史内容不迁移。
- 发布流程改为签出后显式获取标签，避免重复拉取导致失败。

### 文档

- 增加中文变更记录，与英文版同步。

## v0.2.0 - 2026-07-14

### 新增

- 内容块支持复制全文、右键菜单与快捷键，统一 Windows 和 macOS 的复制行为。
- 详情页增加响应头面板，显示已录制的限流、请求标识和重试信息。
- 支持用环境变量覆盖数据目录与配置路径，自测在临时目录里跑，不碰真实设置。
- 新增泳道探针，并列展示主线与子代理的候选信号，便于用真实流量校准分类。

### 变更

- 合并成单一桌面程序：双击打开界面，带参数启动无窗口服务与代理，agent 经 HTTP 操作；源码命令行保留但不打包。
- 移除独立的命令行二进制，它的发行用途由桌面程序的服务模式替代。
- 删掉独立命令行取消后不再使用的端口读取函数。
- 移除没生效的请求头脱敏开关，敏感头始终脱敏，不提供把凭据明文录下来的选项。

### 修复

- 关窗动作接到窗口的同步事件上，确保 macOS 关窗和退出都能还原配置。
- 孤儿恢复先核对配置当前值，只有仍等于本工具写入的地址才还原，避免旧标记覆盖用户后来的修改。
- 启动时执行保留天数清理并返回结果，这个设置不再空转。
- 非流式响应补齐用量、内容块和停止原因的解析。
- 录制写入失败增加计数、日志与红色提示；写盘失败仍然不阻塞转发。
- 统一用量字段的读取，修复时序图和命令行因为读错键而丢失 token 统计。
- 界面展示连接与超时错误的细节，不再只显示错误类别。
- 接通自动启动代理的配置，启动行为遵从用户设置。
- 自测的流式用例改用真实的 token 字段，并补充非流式上游用例。
- 长文本翻译的超时增至 180 秒，错误留在结果区并显示错误码或上游停止原因。
- 界面拦截密钥与地址里的非 ASCII 字符，指出是哪个字符。
- 翻译和解读的结果剥离泄漏的定界符。

## v0.1.0

### 新增

- 发布首个开源版本。

